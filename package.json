{
  "name": "infra-poc-aws-cdk",
  "version": "1.0.0",
  "private": true,
  "bin": {
    "infra-poc-aws-cdk": "bin/infra-poc-aws-cdk.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "synth": "npm run build && npx cdk synth",
    "deploy": "npm run build && npx cdk deploy",
    "destroy": "npm run build && npx cdk destroy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.136.0",
    "constructs": "^10.3.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
